type Task implements Timestamps {
  id: ID!
  name: String!
  fileData: [String]!

  # Interface required
  createdAt: Date!
  updatedAt: Date!
}

# Inputs
input TaskInput {
  id: Int!
  name: String!
  fileData: String!
}